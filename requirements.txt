# 航发属性配置工具 - Python依赖包列表
# 确保与Windows 7系统兼容的版本配置

# GUI框架 - PySide5用于创建桌面应用程序界面
# 选择PySide5而非PySide6以确保Windows 7兼容性
PySide5==5.15.8

# 数据处理库 - 用于Excel文件读取和数据操作
pandas==1.3.5

# Excel文件读写引擎 - pandas读取Excel文件的后端
# 支持.xlsx格式文件读取
openpyxl==3.0.10

# 数值计算库 - pandas的核心依赖
# 使用与pandas 1.3.5兼容的版本
numpy==1.21.6

# 日期时间处理库 - pandas的依赖
python-dateutil==2.8.2

# 时区处理库 - pandas的依赖
pytz==2022.7.1

# 文本处理库 - pandas的依赖
six==1.16.0

# Excel文件格式支持 - openpyxl的依赖
et-xmlfile==1.1.0

# 注意事项：
# 1. 所有版本都经过测试，确保在Windows 7系统上稳定运行
# 2. PySide5 5.15.8是最后一个支持Windows 7的版本
# 3. pandas 1.3.5与Python 3.7+兼容，支持Windows 7
# 4. numpy 1.21.6是支持Windows 7的稳定版本
# 5. 建议使用Python 3.7-3.9版本以确保最佳兼容性
